import { WorkspaceOverlay } from '../workspace-surfaces/WorkspaceOverlay.tsx';

export function AgentSimulationSetup({ agentName, revision, onClose, onContinue }: { agentName: string; revision: string; onClose(): void; onContinue(): void }) {
	return <WorkspaceOverlay reference={{ kind: 'simulation', id: 'agent-definition-setup' }} label={`Test ${agentName}`} onClose={onClose} className="ts-agent-designer__simulation"><header><span>▶</span><div><h3>Test {agentName}</h3><p>Current production path · retained team workday</p></div><button type="button" aria-label="Close simulation setup" onClick={onClose}>×</button></header><div className="ts-agent-designer__simulation-body"><section><h4>Evaluation</h4><p>The current API generates a validated team-inventory seed and scene. Per-agent scenario/profile selection is not exposed yet, so this setup does not claim to isolate one agent.</p><dl><div><dt>Definition context</dt><dd>{agentName}</dd></div><div><dt>Repository base</dt><dd>{revision.slice(0, 12)}</dd></div><div><dt>Authority</dt><dd>Planning-only generated scene</dd></div></dl></section><aside><small>Resolved automatically</small><h4>Current team inventory</h4><p>TreeDX definitions → immutable commit → operation runner → real workday → Agent Atlas</p><strong>Writes remain within the configured evaluation workspace.</strong></aside></div><footer><p>Simulation definitions must be validated and committed in Simulation Bay before launch.</p><button type="button" onClick={onClose}>Cancel</button><button type="button" onClick={onContinue}>Open Simulation Bay</button></footer></WorkspaceOverlay>;
}
