import { useState } from 'react';

export function StructuredSourceEditor({ title, description, source, language = 'yaml', saveEndpoint, expectedBase, projectId = '', projectName = 'Selected project', path = '', onSaved }: { title: string; description: string; source: string; language?: 'yaml' | 'mdx'; saveEndpoint: string; expectedBase?: string | null; projectId?: string; projectName?: string; path?: string; onSaved?: (commit: string) => void }) {
	const [value, setValue] = useState(source); const [selectedPath, setSelectedPath] = useState(path); const [state, setState] = useState<'idle' | 'saving' | 'saved' | 'conflict' | 'error'>('idle'); const [message, setMessage] = useState('');
	async function save() { setState('saving'); setMessage(''); try { const response = await fetch(saveEndpoint, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ source: value, language, expectedBase, projectId, path: selectedPath }) }); const result = await response.json(); if (response.status === 409) { setState('conflict'); setMessage(result.error ?? 'The authoring branch changed. Compare and reload before saving.'); return; } if (!response.ok) throw new Error(result.error ?? `Save failed (${response.status}).`); setState('saved'); setMessage(`Committed ${String(result.commit ?? result.payload?.commit ?? '').slice(0, 12)}`); onSaved?.(result.commit ?? result.payload?.commit); } catch (error) { setState('error'); setMessage(error instanceof Error ? error.message : 'Save failed.'); } }
	return <section className="ts-command-source"><header><div><span>{language.toUpperCase()} source</span><h3>{title}</h3><p>{description}</p></div><button type="button" onClick={save} disabled={state === 'saving' || !projectId || !selectedPath}>{state === 'saving' ? 'Validating…' : 'Validate & commit'}</button></header><div className="ts-command-source__target"><div><span>Project</span><strong>{projectName}</strong></div><label><span>Repository path</span><input value={selectedPath} onChange={(event) => setSelectedPath(event.target.value)} /></label></div><textarea spellCheck={false} value={value} onChange={(event) => { setValue(event.target.value); setState('idle'); }} aria-label={`${title} source`} />{message ? <p className="ts-command-source__message" data-state={state}>{message}</p> : null}</section>;
}
